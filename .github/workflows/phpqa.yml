name: PHP QA — PHPCS + PHPStan (PHP 8.1–8.5)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  # ── PHPCS + PHPStan matrix ──────────────────────────────────────────────────
  phpqa:
    name: phpqa (PHP ${{ matrix.php-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php-version: ["8.1", "8.2", "8.3", "8.4", "8.5"]
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Setup PHP ${{ matrix.php-version }}
        uses: shivammathur/setup-php@ec406be512d7077f68eed36e63f4d91bc006edc4
        with:
          php-version: ${{ matrix.php-version }}
          coverage: none

      - name: Install QA tools (PHPCS + PHPStan)
        run: composer install --no-progress --no-interaction --prefer-dist

      # Advisory for now: the codebase carries a large backlog of mechanical
      # PHPCS style violations (mostly EOL/indent) and PHPStan needs Joomla
      # framework stubs to resolve "extends unknown class" on the plugin
      # entrypoints. These run and surface findings but do not fail the build
      # (tracked as post-launch cleanup). The product is built and runtime-tested
      # by ci.yml independently of this workflow.
      - name: Run PHPCS — plugins/ + component/
        continue-on-error: true
        run: vendor/bin/phpcs --standard=phpcs.xml --report=full -q

      - name: Run PHPStan — plugins/
        continue-on-error: true
        run: vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress --memory-limit=512M

