@{
  ACTIONS = @{
    # Repo-level secrets
    # "GH_TOKEN" = "..."
  }
  ENVIRONMENTS = @{
    # "staging" = @{ "API_KEY" = "..." }
    # "production" = @{ "API_KEY" = "..." }
  }
  DEPENDABOT = @{
    # "NPM_TOKEN" = "..."
  }
  WEBHOOK_SECRETS = @{
    # "https://hooks.slack.com/services/..." = "whsec_..."
  }
}
