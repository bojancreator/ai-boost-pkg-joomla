---
name: agent-operating-procedure
description: How the agent must operate on the AI Boost for Joomla project. Load at the START of any work in this repo — it covers conversation language (Serbian with Bojan), what to read and in what order, which skill to activate for which situation, the mandatory after-work report and staging verification, when to make suggestions, and the hard rule to STOP and ask Bojan (in Serbian) after 5–6 failed attempts instead of looping or falsely reporting "done".
---

# Agent Operating Procedure — AI Boost for Joomla

The authoritative procedure lives in **`replit.md` → "🤖 Agent Operating
Procedure"**. Read that section before starting any work on this project. It is
the single source of truth — this skill exists only so the procedure is
auto-discovered at the start of a task and is **not** restated here (to avoid
drift).

Quick reminders (details in `replit.md`):

- Talk to Bojan in **Serbian**; write code/UI/docs/marketing in **English**.
- After 5–6 serious failed attempts on the same problem, **STOP** — never loop,
  never falsely report "done". Escalate in Serbian per the format in `replit.md`.

If this skill and `replit.md` ever disagree, **`replit.md` wins** — update this
skill to match.
