- [PHP 8.5: never call setAccessible()](php85-no-setaccessible.md) — `ReflectionProperty/Method::setAccessible()` is a no-op since 8.1 and deprecated in 8.5 (try/catch won't swallow E_DEPRECATED); reflect+read/write directly. Also bans non-canonical casts `(boolean)/(integer)/(double)/(real)`. Guarded by `Php85DeprecationContractTest`.
- [Manifest-derived Vue partials and parity tests](manifest-codegen-parity.md) — `tabs/generated/` partials are pure manifest derivations; exclude them from Vue↔ProFeatureRegistry scans and assert their gate-keys via a manifest-level parity test instead, or you get spurious orphans.
- [Pro gating drift in ProFeatureRegistry](pro-gating-drift.md) — manifest `tier=pro` fields gated only by `section:*` wrappers (not by exact key) slip past stripLocked() on Free installs; parity test must walk both `all()` keys and the union of `sectionFields()` values, with allowlists explicit and time-stamped.
- [Integration SDK — registry + dispatcher invariants](integration-sdk-architecture.md) — dynamic-over-fallback merge in `integrations()`, lockstep PluginRegistry↔IntegrationRegistry reset, FilterDispatcher preserves partial mutations on throw.
- [AI Boost Pro UI gating rule](aiboost-pro-gating-rule.md) — one gate `PluginRegistry::isProActive()`: perpetual `pro_activated` flag (set once on first verify, never cleared) → Pro forever; expiry only pauses updates/support. Precedence dev_force_free_tier→pro_activated→dev_license_preview→false. UI+save+sitemap delegate.
- [Pro package installer must enable its own plugins](pro-package-installer-enable.md) — pkg_script_pro postflight must enable the 5 aiboost_*_pro plugins (and free script must create the mod_aiboost_health instance); else paying customers get zero Pro output.
- [AI Boost staging audit gotchas](aiboost-staging-audit-gotchas.md) — Pro plugins disabled-by-default (leaks are latent); Falang corrupts endpoint HTTP status (audit by body) + redirects non-/sr/ URLs; settings.save strips Pro keys.
- [Joomla 6 admin SPA browser automation](joomla-admin-browser-automation.md) — dismiss guided-tour modal first, drive hash router via JS not goto URL, skip networkidle waits, or the com_aiboost SPA walk redirects/hangs.
- [SQL LIKE prefix scans are sql_mode-fragile](sql-like-prefix-scan.md) — never `LIKE 'aiboost\_%'`; coarse escape-free WHERE + `str_starts_with` prefix filter in PHP, or NO_BACKSLASH_ESCAPES falsely flags rows.
- [AI Boost Vue admin dual-mount + settings sub-tab nav](aiboost-vue-admin-dual-mode.md) — admin mounts as SPA (router+Sidebar) OR legacy standalone (no router); App.vue must support both. Settings sub-tabs are query-driven, not routes.
- [Vue 3 — no Window/DOM in reactive data](vue-window-in-reactive-state.md) — "__v_isReadonly from Window" SecurityErrors come from storing Window/DOM/3rd-party objects in data()/ref(); use non-reactive `this._x` or markRaw().
- [Sitemap/JSON output leak from third-party plugins](sitemap-thirdparty-output-leak.md) — another plugin (Falang PHP 8.5) can flush warning HTML before our handler; fix = drain all buffers for JSON, scoped import-time display_errors=0 for sitemap URLs.
- [License reconcile — install_id is the no-false-positive anchor](license-reconcile-no-false-positive.md) — lapsed past purchasers recovered by server-matching bound install_id (unguessable UUID) only; domain is spoofable so never sufficient; trust server verdict, all anti-false-positive logic server-side.
- [Joomla package postflight fires on uninstall](joomla-postflight-fires-on-uninstall.md) — Joomla calls pkg_script postflight() with $type='uninstall' too; any artifact (re)generation must sit inside the install/update/discover_install guard or it undoes the uninstall cleanup.
- [Settings UI/backend default mismatch](settings-default-mismatch.md) — absent toggle key reads ON in PHP (`?? 1`) but OFF in the Vue checkbox; add it to App.vue DEFAULTS or both sides disagree. Global master switch removed in v0.70.0.
- [Deployed ZIP can lag behind repo source](deployed-zip-lags-source.md) — when a staging/CI check fails on behaviour the source says is fixed, suspect a STALE deployed build (rebuild + reinstall) before deep-debugging the test or source.
- [settings.save is a full-replace endpoint](settings-save-full-replace.md) — `SettingsController::save()` replaces the whole `main` settings row (only POST keys + a small carry-forward whitelist survive); never do partial programmatic saves or you silently wipe sibling keys.
- [Every settings-blob writer must be read-modify-write or full snapshot](settings-writer-rmw-pattern.md) — `#__aiboost_settings` 'main' is one JSON blob; a subset-replace writer wipes siblings. Enforced by `SettingsWriterRmwContractTest` (discovery guard + load-before-write). Dead `saveSettings()` = the anti-pattern (BACKLOG: delete).
