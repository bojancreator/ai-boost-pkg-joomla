# STATUS — AI Boost for Joomla (single live board)

**This is the one place that tells the truth about *where we are now*.** Read it first at the start of
every chat; update it at the end of every chat. It replaces the old `ROADMAP-v0.5.md` as the command post.

- **What's left to do** → `BACKLOG.md`
- **How we work** (rules + Definition of Done) → `OPERATING.md`
- **Why / history** (decisions, past verification) → `ROADMAP-v0.5.md` (ARCHIVE) and `docs/`

---

## Now

| Field | Value |
|---|---|
| **Code version** | `0.88.0` (2026-06-29) — source of truth `component/Version.php` |
| **Branch** | `refactor/structural` (big-project stream — analyses 0013/0014 + arch 0015 + cleanup 0017 + T1 design 0018/S0 0019; PUSHED to `origin`). Earlier admin-UI work was on `design/admin-ui-refresh`; base `main` |
| **Latest (2026-06-29)** | **T1 · S8 (order 0029, v0.88.0): per-page noindex MECHANISM — NEW opt-in, default-OFF (Bojan chose Option A).** A new default-OFF setting **`markdown_alternate_noindex`** (`Manifest/aeo.php`, Free tier, AEO tab): when ON, the **Markdown/AI alternate** of a page (its `.md` / `?markdown=1` copy — a duplicate of the HTML page) sends **`X-Robots-Tag: noindex`** so it never competes with the original in search (closes architecture B2). **Option A:** the per-page noindex mechanism is fully wired (X-Robots header + a dormant head robots-meta + sitemap exclusion all read `PageContext::indexable`), but the ONLY thing that becomes non-indexable is the Markdown alternate — **NO normal HTML page is hidden** (the HTML-page path is wired but dormant until a future per-page flag = Option C, deferred; auto-noindex page types = Option B, deferred). New `PageContext::withIndexable()` immutable wither; `AiBoostAeo::resolveRequestIndexability()` flips indexable ONLY for a Markdown request when the setting is on; the Markdown response emits noindex independent of the Pro X-Robots feature (so it works on Free too). Health entry `info_aeo_markdown_noindex_active` (info — never a failure). **OFF = byte-identical to v0.87.68.** **Verified live on clean j6pro (no Falang/4SEO) via a real OFF↔ON↔OFF settings round-trip:** with ON, `/sr/` home AND article `?markdown=1` → `text/markdown` + **`X-Robots-Tag: noindex`**; the HTML pages → **unchanged (no X-Robots)**; restored to OFF → noindex gone. Health on Pro staging = **94 (unchanged from S7)** + the new info entry present. Suite **513 PHPUnit + 3/3 standalone green**, codegen `--check` + Pro-leakage STRICT green; **red-green** = the S1 X-Robots contract pinned the old hardcoded `index, follow` → flipped RED on the indexable-driven change → GREEN at the new contract; 4 new tests (`withIndexable` ×3 + the Markdown-noindex contract). Installed Pro (staging) + Free (offroadbalkans) + clean j6pro. **Next: S9 (cleanup + the no-new-inline-gate contract test) — the last T1 slice.** Report: `_handoff/outbox/0029-…`. — Earlier: **T1 · S7 (order 0028, v0.87.68): THE behaviour change — "the homepage is ALWAYS the homepage" (Bojan-reviewed before/after).** The menu `home=1` page now classifies as HOME whatever it is built from (single article, featured/blog, category blog), incl. the localized `/sr/`,`/en/` home. **Title/meta** select via new `AiBoostCore::resolvePageType()` (the resolver, homepage-first; legacy `detectPageType()` kept ONLY as the absent-resolver fallback). **Schema (Pro):** `SchemaProBuilder` article gates → `isArticlePage()` (== `PageContext::isArticle()`, homepage-aware) — a single-article home emits NO Article/Event/HowTo/auto-FAQ; the Free WebSite/home graph wins. **Social (Pro):** `OgTagProDecorator` article gate homepage-aware → home keeps `og:type=website`, no `article:*`/author/datePublished. **Verified live on Pro staging (its home IS a single-article home):** `/en/` & `/sr/` home — `og:type` **article→website**, `<title>` **article-template→home-template** (`…OFF ROAD SERBIA\|Uncategorised…`→`4X4 Serbia Crew\|Welcome`), `article:published_time` **1→0**; **every inner page byte-identical** (petrus-2025 + the other single-article menu items stay `og:type=article`). Schema is deferred to YOOtheme/4SEO on staging (not visible there) but driven by the SAME `isHomepage` gate as og:type (so the og:type flip proves it) + locked by a new unit test. Suite **509 PHPUnit + 3/3 standalone green**, codegen `--check` + Pro-leakage STRICT green; **red-green** = the S1 title/meta-coupling contract flipped RED on the change then GREEN at the new homepage-first expectation. Installed Pro+Free. **Next: S8 (per-page noindex, opt-in default-OFF).** Report: `_handoff/outbox/0028-…`. — Earlier: **T1 · S6 (order 0027, v0.87.67): LANGUAGE facts onto the resolver — behaviour-preserving.** The active page language (L1) and the site-default CONTENT language (L2) now come from the ONE resolver source. **L1** — every front-end Pro emitter reads `PageContext::language` (= `getActiveLanguage()`, same per-request value) instead of `$ctx->getActiveLanguage()`: `SchemaProBuilder` (×7, via a stored `$this->lang`), `OgTagProDecorator` (×2), `LlmsTxtProGenerator` (×2 non-override; `AiBoostAeo` threads the resolved context into its 3 constructions). All carry a `?PageContext` null-fallback to `$ctx` (unit/standalone callers byte-unaffected). **L2** — `AiBoostIntFalang::primaryLanguageSef()` reads `PageContext::siteDefaultLanguage` (PRIMARY = `com_languages` `site`, unchanged on every functional multilingual site) via a guarded `resolveSiteDefaultLanguage()` (legacy `com_languages` read kept as the absent-resolver fallback). **First STOPPED at the L2 empty-`site` divergence (report 0027 v1); Bojan's fallback-direction rule resolved it:** a FRONT-END signal falls back to a FRONT-END default — the empty-`site` edge now follows the STABLE front-end config default (`$app->get('language')` on the SITE app — confirmed stable/not-active-request by B7), filtered to a PUBLISHED language by `resolvePrimaryLanguageSef()`; never the admin default, never active-request, never hardcoded 'en'. Memory `joomla-site-default-vs-global-language` updated with the rule. **L3 (`LanguageDetector`) untouched.** **LANGUAGE golden diff BYTE-IDENTICAL before↔after on Pro staging** (multilingual sr/en/ru): hreflang set + **x-default→/sr/** + `og:locale` (sr_YU/en_GB/ru_RU per active lang) identical on /sr/, /en/, /ru/ + an article; sitemap = 104 x-default all →/sr/ (consistent). Free (j5free): language output is Pro-only → none before/after (no regression); installed + renders. Suite **508 PHPUnit** (2 new `OgTagProDecoratorLanguageSourceTest` cases — og:locale follows PageContext vs ctx; **red-green proven**) **+ 3/3 standalone**; phpstan 192=192 (zero new); phpcs zero new (the big Pro service files' pre-existing PSR-12 debt unchanged); codegen `--check` + Pro-leakage STRICT green. Installed Pro (staging) + Free (j5free); Health unchanged (staging 94, j5free 80 — same as S5). **Next: S7 ⚠ — THE behaviour change: unify homepage detection on the menu `home=1` flag (needs Bojan's sign-off).** Report: `_handoff/outbox/0027-…`. — Earlier: **T1 · S5 (order 0026, v0.87.66): CANONICAL onto the resolver — behaviour-preserving.** The canonical-URL logic (URL-map lookup + bare scheme://host/path fallback) now lives in the shared `PageResolver`, and `AiBoostCore` reads `PageContext::canonical` instead of its private `resolveCanonical()`. **Threading choice (the S5 design point):** the resolver still takes no settings — the raw `canonical_url_map` is threaded in only by the canonical consumer via a new optional `resolve(?string $canonicalUrlMap = null)` arg (default null = no map → bare URL, so the S2/S3/S4 callers are byte-unaffected); a map hit returns a context with the mapped canonical **without mutating the memoised base** (`PageContext::withCanonical()`), so the answer is independent of which consumer resolves first (no plugin-ordering dependence). The legacy `resolveCanonical()` is KEPT as the guarded fallback (Page classes absent / resolve throws). **Byte-faithful detail:** the resolver derives the bare URL by `parse_url()`-ing the AppContext URL (CMS-neutral) → scheme://host/path with query/fragment stripped, matching the old `Uri` logic exactly. **CANONICAL golden diff BYTE-IDENTICAL before↔after, incl. a URL-map hit, on Free (j5free) AND Pro (j6pro)** — home (bare) + an inner page (bare) + a seeded-map page (→ mapped target) all identical; temp map seeded then restored on both. Staging (Pro, v0.87.66 installed): canonical is owned by **4SEO** there (AI Boost defers — the consolidated block carries no canonical note), so staging canonical is unchanged before↔after (regression-safe) and the meaningful map exercise was done on the clean no-4SEO sites. Suite **506 PHPUnit** (7 new `PageResolver` canonical cases — bare query-strip + exact/prefix map hit + no-hit/invalid fallback + no-mutate-base; **red-green proven**: 4 fail on pre-S5 source) **+ 3/3 standalone green**; phpstan unchanged (192 = 192, zero new); phpcs clean on the 3 Page files (AiBoostCore's pre-existing PSR-12 debt unchanged, zero new); codegen `--check` + Pro-leakage STRICT green. Installed Free (j5free) + Pro (j6pro + staging.offroadserbia.com); Health scores reflect pre-existing site config (no S5-related entry touched — canonical computation is not a Health input). **Next: S6 (expose language facts L1–L2 via PageContext).** Report: `_handoff/outbox/0026-…`. — Earlier: **T1 · S4 (order 0024, v0.87.65): INDEXABILITY unified — the four bulk enumerators now share ONE authority, zero URL change (Option A).** Bojan chose **Option A** (behaviour-preserving centralisation) after the S4 needs-input gate. Extended `IndexabilityPolicy` with the design §2.3 **parameterised SQL-constraint contributor** `itemWhereClauses()` (the bulk counterpart S0 didn't build — per-row `isIndexableItem()` can't drive SQL+LIMIT enumerators). Migrated all **six** fetch methods to build their item-indexability WHERE via that one method, **each called with EXACTLY its current parameters** (I1 sitemap = state+sitemap-window+guest-access on article AND category; I2 news = state+recent-window; I3 llms.txt = state+llms-window; I4a llms-Pro-recent = state only; I4b llms-full = state+access=1; I4c llms-full categories = published+access=1). One authority, one place to tighten later; **zero SEO/URL change**. The genuine cross-list unification (Option B — making the four consistent, which WOULD change some URLs) is **deferred** (new BACKLOG line, tied to S8). **URL-SET golden diff IDENTICAL** — Pro staging before↔after on `sitemap.xml` (110) + `sitemap-news.xml` (0) + `/llms.txt` (26) + `/llms-full.txt` (131) (clean v0.87.64 baseline) ✅; Free same-version isolation (0.87.65 WITH vs WITHOUT S4 on j5free: sitemap 5 + llms 13) ✅. Suite **500 PHPUnit** (6 new `itemWhereClauses` cases, red-green proven) **+ 3/3 standalone green**; S1 I1–I4 migration-map pins updated to assert delegation; phpstan unchanged (192 pre-existing local Joomla-resolution errors, identical with/without); codegen `--check` ✓, Pro-leakage STRICT ✓. Installed Pro (staging.offroadserbia.com) + Free (joomla5-free.testmyweb.info); Health 94/100, 77 checks, 0 hard-fails (unchanged). **Next: S5 (canonical onto the resolver).** Report: `_handoff/outbox/0024-…`. — Earlier: **T1 · S3 (order 0023, v0.87.64): SOCIAL/OpenGraph layer migrated onto the resolver — behaviour-preserving.** The Social/OG layer now reads the wired `AdapterRegistry::pageResolver()->resolve()` `PageContext` instead of re-deriving page-type from `JoomlaAppContext`: `AiBoostSocial` resolves the context once (guarded; null-falls-back to `$ctx`) and feeds it to `OgTagBuilder` (P11 — seeds the props `context` block from `PageContext::option/view/rawId`) and `OgTagProDecorator` (P12 — the article gate at `:123` reads `PageContext::option/view/rawId`, falling back to `props['context']` when absent). **Same S2 pattern: RAW primitives, NOT homepage-first `isArticle()`** — so the single-article-home case (homepage emits `og:type=article` + `article:*`) is preserved byte-for-byte; that semantics change is S7. All three classes keep a `?PageContext` ctor param defaulting null → fall back to `$ctx`/`props['context']` (the standalone `test-og-pro-decorator` + `OgTagBuilderTest` unaffected). **Golden diff: OG/Twitter BYTE-IDENTICAL** — Pro staging before↔after on `/en/`+`/sr/` homepages + 3 article pages (clean v0.87.63 baseline) ✅; Free same-version isolation (0.87.64 WITH vs WITHOUT S3 on j5free home/about/category) ✅. Suite **494 PHPUnit + 3/3 standalone green** (incl. `test-og-pro-decorator`), phpstan unchanged (192 pre-existing local Joomla-resolution errors, identical with/without), codegen `--check` ✓, Pro-leakage STRICT ✓. Installed Pro (staging.offroadserbia.com) + Free (joomla5-free.testmyweb.info); Health on Pro staging = **94/100, 77 checks, no fatal** (unchanged). **Next: S4 (indexability — bulk IndexabilityPolicy for sitemap/news/llms).** Report: `_handoff/outbox/0023-…`. — Earlier: **T1 · S2 (order 0022, v0.87.63): SCHEMA layer migrated onto the resolver — behaviour-preserving.** The Schema layer now reads the wired `AdapterRegistry::pageResolver()->resolve()` `PageContext` instead of re-deriving page-type/homepage from `JoomlaAppContext`: `AiBoostSchema` resolves the context once and feeds it to both builders; `SchemaProBuilder` sources the article-gate primitives (P3–P8) from `PageContext::option/view/rawId`; the Free `SchemaBuilder::buildWebSite()` homepage gate (P10) reads `PageContext::isHomepage`. **Crucially used the resolver's RAW primitives + injected isHomepage — NOT the homepage-first `isArticle()` semantics** — so the single-article-home case (where Article+FAQ+HowTo schema co-emit WITH WebSite) is preserved byte-for-byte; that homepage-semantics change is deferred to S7. Both builders keep a `?PageContext` ctor param defaulting null → fall back to `$ctx` (unit tests unaffected). **Golden diff: schema BYTE-IDENTICAL** — Pro staging before↔after on `/en/`+`/sr/` homepages + 3 article pages (clean v0.87.62 baseline) ✅; Free isolation diff (same-version 0.87.63 WITH vs WITHOUT S2 on j5free home/article/category) ✅. Suite **494 PHPUnit + 3/3 standalone green** (incl. the S1 single-article-home divergence test still green), phpstan unchanged (192 pre-existing local Joomla-resolution errors, identical with/without the change), codegen `--check` ✓, Pro-leakage STRICT ✓. Installed Pro (staging.offroadserbia.com) + Free (joomla5-free.testmyweb.info); Health on Pro staging = **94/100, 77 checks, no fatal** (unchanged). **Next: S3 (Social — OgTagBuilder/OgTagProDecorator).** Report: `_handoff/outbox/0022-…`. — Earlier: **T1 · S1 (order 0021, test-only — version stays v0.87.62): characterization baseline locked.** 33 new tests pin the CURRENT behaviour at every site the resolver will later replace (migration map H1–H2, P1–P12, I1–I6, C1, L1–L3): resolver-equivalence for the article/homepage/category/canonical gates, a behavioural SchemaProBuilder gate-exclusion test, and source-contract pins (HomepageDetectionContractTest idiom) for the Factory/SQL-coupled sites (detectPageType, resolveCanonical, the 4 enumerator indexability filters, X-Robots/Markdown, language). Suite **494 PHPUnit + 3/3 standalone green**, codegen `--check` ✓, red-green proven; **nothing in the product changed → no build/staging** (test-only). **Finding flagged to S2:** the inline article gate is homepage-agnostic but the resolver classifies homepage-first → divergence on a single-article homepage (S2 must preserve or decide). Next: S2 (first consumer migration — schema). Report: `_handoff/outbox/0021-…`. — Earlier: **v0.87.62 (order 0019): T1 · S0 — the dormant PageResolver.** Implemented the first slice of the approved T1 design (`docs/analysis/T1-resolver-design.md`): a new CMS-neutral `component/lib/src/Page/` family — `PageType` (enum), `PageContext` (the one "where am I?" value object: type, entity kind/id, the single homepage truth, language + site-default language, canonical, the single indexability verdict), `PageResolver` (reads ONLY via `AppContextInterface` + the `Cms` DatabaseAdapter — no direct Joomla calls), and `IndexabilityPolicy` (per-page noindex capability default-OFF per D2 + the one item-level indexability rule for the future bulk enumerators). Wired via new `AdapterRegistry::pageResolver()`/`setPageResolver()` + `AdapterBootstrap`. **DORMANT — consumed by nobody yet**, so the rendered site is unchanged: front-end head block / schema JSON-LD / sitemap **byte-identical BEFORE↔AFTER on Pro `/en/` and Free `/`** (only a live article view-counter differed). The bootstrap constructs the resolver every request (never calls `resolve()`), which also proves the new classes load with no error. 19 new unit tests (resolver classification + indexability), **red-green proven**; suite **461 PHPUnit + 3/3 standalone green**; codegen `--check` + Pro-leakage STRICT green; installed Pro+Free. No manifest/settings/Health-registry change → Health unaffected. **Next: S1 (characterization tests).** Report: `_handoff/outbox/0019-…`. — Earlier: **v0.87.61 (order 0017): SAFE cleanup batch — built, installed Pro+Free, verified live.** (1) **Removed the AI meta tags entirely** — the unbackable `ai-content-verified`/`ai-content-optimized` + the bundled `llms-txt` pointer (the whole `aeo_ai_meta_enabled` "AI Signals" feature: emission, manifest field, Vue UI, generated partial, en-GB ini, save whitelist, Health check + CATEGORY, DocumentInspector signal + test, the dead HeadBlockBuilder dedup branch). **Verified live GONE on both Pro `/en/` and Free `/`** (0 `ai-content-`/`llms-txt` metas; AI Boost block + the kept Markdown-discovery link still render). (2) **Reclassified 4 Free→Pro** (`enable_per_article_fields`, `enable_article_og_type`, `fb_app_id`, `twitter_site_handle`) — manifest `tier='pro'` + codegen; consumed only by the Pro-only `OgTagProDecorator`. Red-green proven on `SettingsSaveDefinitionTest`; Free-lock confirmed live (verify-pro-lock: 10 locked cards). (3) **Deleted dead Pro-gate code** — the `ProGate` trait (file removed) + `AbstractService::isProTier()/getLicenseTier()`; switched the 3 DISPLAY readers (`Dashboard/HtmlView::checkIsProEnabled`, `HealthCheckService::infoArticleCustomFieldsPro`, `mod_aiboost_health`) from raw `license_tier` → `PluginRegistry::isProActive()`. **Decision: `license_tier` KEPT as a Health diagnostic only** (still written by `saveLicenseState`, surfaced by `info_license_tier` + the legacy-detection warning), never a gate. (4) **DRY** — one shared `OgCustomFieldCatalog` now feeds both `pkg_script::ensureOgCustomFields()` and `SettingsController::repairOgFields()`; fixed the stale `_pro`-home docblocks in `CustomFieldReader`/`OgTagProDecorator`. (5) **Refreshed gating docs** — `CLAUDE.md` + the joomla-development skill now describe the real model (single `pro_activated` flag via `isProActive()`; `stripLocked()` no-op; save fence = `SYSTEM_PRESERVED_KEYS` + physical Free build). Suite **442 PHPUnit + 3/3 standalone green**; codegen `--check` + Pro-leakage STRICT green; Health on Pro staging = 94/100, 77 checks, no "AI Meta Tags" row, no fatal; Dashboard reads Pro. Report: `_handoff/outbox/0017-…`. — Earlier: **v0.87.60 (order 0006): B2 + B8 + B7 all fixed & verified live; B5 reclassified.** **B2** — WebSite/SearchAction now only on the real homepage (`isHomepage()` reads the active menu item's `home` flag, not a path/featured heuristic). **B8** — og:image/twitter:image no longer carry the `#joomlaImage://…` fragment (clean URL + real dims). **B7** — head/sitemap x-default now follows the **SITE default content language** (Language Manager → Installed → Site → Default = `ComponentHelper::getParams('com_languages')->get('site')`, a setting DISTINCT from the global config default), verified live: head = en-gb + ru-ru + x-default→/sr/ on every page (en-gb self-alternate restored), sitemap consistent. New memory `joomla-site-default-vs-global-language.md`. **B5** — "duplicate Article" is the YOOtheme template's @graph (not internal) → order 0007. **BACKLOG**: manual_faq_scope. All red-green tested; suite 443 + standalone 3/3; installed Pro+Free. Report: `_handoff/outbox/0006-…`. — Earlier: **v0.87.59 (order 0006): bug fixes B2 + B8 shipped; B7 dynamic; B5 reclassified.** **B2** — WebSite/SearchAction was emitting on every page because `JoomlaAppContext::isHomepage()` used a fragile path/featured heuristic; now reads the active menu item's `home` flag (verified live: WebSite only on the real homepage). **B8** — og:image/twitter:image carried a `#joomlaImage://…` fragment; `OgTagBuilder::normaliseImagePath()` now strips it (verified live: clean URL + real 1080×1350 dims). Both red-green tested. **B5** — the "duplicate Article" is the YOOtheme template's `@graph` (outside the AI Boost block), NOT an internal duplicate; AI Boost emits exactly one Article → reclassified to order 0007. **B7** — head/sitemap x-default now follows the native Joomla site-default language dynamically (`BridgeDetector::resolvePrimaryLanguageSef`, unit-tested incl. non-English-default). OPEN: on staging the native Joomla default is en-GB while Falang serves Serbian at root, so x-default resolves to /en/ there — needs owner decision (set the site's Joomla default to sr-YU → x-default→/sr/ automatically, or accept native=en). **BACKLOG**: manual_faq_scope (finish/remove). Suite 443. Report: `_handoff/outbox/0006-…`. — Earlier: **v0.87.57 (order 0005): fixed the one real PHP 8.5 deprecation in our code + separated real bugs from config artifacts.** Removed `ReflectionProperty::setAccessible()` in `AiBoostAeo::detectMarkdownRequest()` (no-op since 8.1, deprecated 8.5); red-green verified live on PHP 8.5 staging (only Falang's own remain). New `Php85DeprecationContractTest` guards setAccessible + non-canonical casts; suite 434 green; installed Pro+Free. **Findings (recorded in `docs/feature-verification.md`):** canonical = NOT our bug (works on clean Pro everywhere; Falang/YOOtheme strip it on multilingual sub-pages); /llms-sr.txt = by design (SR is default lang); B2 WebSite-everywhere = real bug (isHomepage heuristic, record-only); HowTo actually article-only (works); FAQ manual items don't emit on staging (legacy q/a-key data — likely stale import) + manual_faq_scope dead. **Part C live-verified (via temp published content, since removed; events_category_id restored): Event ✓, BlogPosting/NewsArticle ✓, Author Person ✓; BreadcrumbList code-correct but Joomla pathway empty on this YOOtheme template (untriggerable here).** Report: `_handoff/outbox/0005-…`. — Earlier (2026-06-25): **v0.87.56: TikTok added to sameAs, Pinterest removed.** TikTok URL now appears in Schema.org `sameAs` array (was silently dropped); `social_pinterest` removed from 6 code locations (SettingsSaveDefinition ×2, HealthCheckService, LlmsTxtGenerator, LlmsTxtProGenerator, .ini). 431/431 tests pass. **Korak 2 (full feature verification) COMPLETE** — all-on config on staging.offroadserbia.com (Pro + Falang + EN/SR/RU). 9 bugs recorded in `docs/feature-verification.md`; none fixed (record-only pass). Free staging Pro-gating PASS; J5 working. Report in `_handoff/outbox/0002-full-feature-verification.md`. — Earlier (2026-06-24): **Regression net COMPLETE on branch `design/admin-ui-refresh`** — guards for #14/#16/#2/#8, suite 431. Findings: #14 was a live bug (22 toggles lied, fixed). #8 found a narrow live bug (pkg_script LIKE broke under NO_BACKSLASH_ESCAPES → ex-Pro plugins not disabled on Pro→Free on NBE hosts; runtime isProActive still gated emission, so no Pro leak — fixed via ESCAPE clause). #16/#2 locked already-safe behaviour. Branch NOT merged to main. Open BACKLOG from this work: merge-on-existing save hardening; 3+2 admin-display license_tier drift; SQL LIKE → PHP-filter convergence (3 sites + ErrorsController); UI colour tokens; integration toggle upsell-gating. — Earlier (v0.87.55): **#14 fixed — 22 default-ON toggles now read ON instead of lying/turning off.** The Vue admin's `App.vue` DEFAULTS mirrored only a handful of toggles, so every manifest toggle with `default='1'` that was absent from the saved blob rendered OFF in its checkbox while the backend treated it as ON (`?? 1`) — the UI lied, and a plain Save could persist OFF. Fix (frontend-only): mirrored the **22 divergent default-ON keys** into `App.vue` DEFAULTS as `'1'` (enable_schema/sitemap/robots/opengraph/search_action/twitter_cards, article_/website_schema_enabled, page_type_auto_detect, faq_auto_detect, include_articles/categories/menu_items, ping_google/bing, enable_article_og_type, enable_per_article_fields, llmstxt_enabled, aeo_ai_meta_enabled, auto_domain_detection, enable_canonical, redirect_404_log_enabled). New `ManifestDefaultsParityTest` (PHPUnit) derives the default-ON set from the manifests and asserts each is in DEFAULTS — **red-green proven** (remove one → fails naming it; restore → green). **Documented exception:** the 2 integration master toggles (`integration_falang_enabled`/`integration_yootheme_enabled`) are intentionally NOT mirrored — they wait for the BACKLOG "integration master toggles shown locked (upsell)" item — and the test excludes them by name (+ a guard test that the exception can't rot). Verified: PHPUnit **422 green** + standalone 3/3; deterministic merge proof (all 22 ON on load AND after save+reload; old build read all 22 OFF); **staging v0.87.55 subagent eyes-on PASS** — enable_schema/sitemap/robots/opengraph read ON on fresh load and stay ON after a partial save + reload. — Earlier (v0.87.54): **Integration card options now expand INLINE — the modal is gone.** Owner ask: the "Options" button on the Multilingual + YOOtheme Pro cards opened a pop-up modal; replace it with a non-overlay way of opening options, then point the Dashboard "Multilingual site detected" notification at it. Done: `IntegrationsPage.vue` now renders Options as a second inline accordion (same `ab-int-acc` style as "What this does"), reusing the previously-dead `openOpts`/`toggleOpts` scaffolding; the fields + ProGate(pro) + status + "Save options" moved inline; the whole `.ab-int-modal` block + `optionsModalItem`/`openOptions`/`closeOptions` + modal CSS removed. Added `data-int-key` on each card + a deep-link handler in `mounted()`: `#/integrations?open=<key>` auto-expands that card's options and scrolls to it. `NotificationService.php` multilingual notice re-pointed `#/settings?tab=sitemap&field=enable_hreflang` → **`#/integrations?open=falang`**, label "Configure hreflang" → "Open Multilingual options". Cleanup: removed the dead `multilingualBannerHref`/`multilingualBannerTarget` computeds in `DashboardApp.vue` (still pointed at the old sitemap field). Verified on staging both themes via `scratchpad/verify-integrations-inline.mjs` (subagent eyes-on PASS): no modal, Multilingual card auto-expands inline from the deep-link in light+dark, YOOtheme expands inline, Save present, notif href correct. `composer test` 3/3 + Pro-leakage STRICT green. **Open (flagged to owner, NOT done here):** Sitemap tab still has legacy `enable_hreflang`/`hreflang_sitemap` that overlap the Multilingual card's `falang_hreflang_sitemap` — possible later consolidation. — Earlier (v0.87.53): **All plugin/integration toggles now apply without a manual reload.** Owner: "this should hold for ALL plugins." Root cause: the SPA caches each screen's data per-route (`useLegacyGlobals` — `isLegacyGlobalsReady`) and never re-derives on revisit, so a toggle stayed stale elsewhere until a full reload. Fix: added `invalidateLegacyGlobals(url)`; after a successful toggle, both `IntegrationsPage.toggleIntegration` and `DashboardApp.doToggle` invalidate the **Dashboard + Integrations** cached views (NOT Settings — remounting it would discard unsaved edits) so they re-fetch + remount with fresh data on next visit. The per-field Translation UI keeps its instant `multilangActive` optimistic flip (v0.87.52). Verified both themes via `verify-multilang-reactive.mjs` (ON→OFF flips 12 dropdowns→12 hints, no reload; the dark pass also confirmed the cache re-fetched fresh server state on revisit). 420 tests green. — Earlier (v0.87.52): Multilingual toggle reactive. (v0.87.51): Multilingual integration pass — relabel + true enabled-state + Translation-UI gate. Owner follow-up: flipping the Multilingual switch on the Integrations card didn't apply elsewhere until a page reload (the per-field Translation UI stayed stale, because `multilangActive` was loaded once per page). Fix: `IntegrationsPage.vue applyToggle()` now also sets the shared `multilangActive` ref (from `useTranslations`) when the `falang` integration is toggled — so the per-field dropdowns hide/show instantly across the SPA, and it tracks rollback too. Verified both themes via `scripts/verify-multilang-reactive.mjs` (saveToggle intercepted/faked so the live integration is never touched): ON→OFF flips 12 dropdowns → 12 hints with NO reload, and OFF→ON back. — Earlier: **Multilingual integration pass (v0.87.51, verified both themes).** Four owner points on Integrations → Multilang: (a) **renamed** the card "Multilang" → **"Multilingual"** + provider **"Native Joomla & Falang"** (was bare "Falang") — descriptor in `AiBoostIntFalang.php` + static tile in `IntegrationDetectorService.php`; (b) **true enabled-state** — `master_enabled` now requires the bridge plugin actually published in `#__extensions` (was setting-only), so unpublishing `aiboost_int_falang` in Joomla → Plugins correctly flips the card to disabled; (c) **Translation-UI gate** — new server flag `multilang_active` (bridge published + master on + 2+ languages) on `settings.getLanguages`; `TranslationExpander.vue` hides the per-field dropdowns when inactive and shows a compact "Turn on Multilingual to translate →" hint → `#/integrations` (stored translations untouched); (d) verified: ON→12 dropdowns, OFF→12 hints, card reads "Multilingual"/"Native Joomla & Falang". **Deploy note:** the bridge descriptor lives in the Pro plugin `aiboost_int_falang` (NOT in the Free `pkg_aiboost`), so it was deployed via a separate `--integration=falang` build + `install-to-staging --zip` of the `_pro` ZIP. New `scripts/verify-multilang-gate.mjs`; `FalangBridgeParityTest` label assertion updated to "Multilingual". (#1 unpublish→disabled verified by code; live unpublish test skipped to avoid disrupting the live multilingual site.) — Earlier today: **FAQ Manual Items → per-item repeater (v0.87.50).** Schema #4: `SchemaTab.vue` "Manual FAQ Items" was a raw JSON textarea but translations were per-item — inconsistent. Per owner (chose "both per-item"), replaced the JSON textarea with a Question/Answer repeater (Add/remove, per-row Translations expanders) mirroring the existing services repeater (`parseFaqItems`/`faqRows`/deep-watch→`s.faq_items`/`addFaqItem`/`removeFaqItem`). **No storage/consumer change** — `s.faq_items` still serialises to the same `[{question,answer}]` JSON and per-item translation keys (`faq_{idx}_q/_a`) are unchanged, so existing items + translations carry over with zero migration (verified on staging: 2 items + RU translations loaded). New `scripts/verify-faq-repeater.mjs`. — Earlier today: **Notifications panel — Part A done + verified (v0.87.47).** New `component/lib/src/NotificationService.php` assembles a deduped, severity-sorted Dashboard notification list by REUSING cheap existing signals (licence state, ConflictDetector, plugin-enabled state, settings flags, robots.txt read, backup timestamp, setup flags, multilingual) — no new detection engine; deep probes stay on Health. Wired via Dashboard `HtmlView`/`default.php` → `window.aiBoostDashboard.notifications`. `DashboardApp.vue` panel rebuilt data-driven: **critical pinned at top + warnings/info in a collapsible counter panel**, each with Fix deep-links + dismiss (reuses `health.dismiss`). Removed the old hard-coded conflict cards / backup banner / multilingual banner (folded in). **Caught + fixed a false positive:** the sitemap is served dynamically (no static file) so a `file_exists('sitemap.xml')` check would always warn — removed it (sitemap reachability stays a Health-only live probe). 11 `NotificationServiceTest` cases; full suite green (420 tests). Next: **Part B — in-component "What's New" changelog** + post-update highlight; website `/changelog` stays planned. **Part B — in-component "What's New" changelog page + post-update highlight DONE (v0.87.48), verified both themes** (new `changelog.js` single-source + `ChangelogPage.vue` at `/changelog` + nav item; Dashboard "updated to vX" highlight cleared by `dashboard.markVersionSeen` writing `last_seen_version`; website `/changelog` stays planned). (Prior today: Help `c914d3f`; Schema Types `2c1ea07` + CI fix `092164b`.) ⟶ earlier detail: `HelpPage.vue` rebuilt problem-first per Bojan: groups *I have a problem · I want to do something · Is my site ready · Still stuck*; each item = plain "what it means" + one-click buttons into the matching screen (Health/Conflict/Analyzer/Quick Setup/Import/Settings). Dropped the bespoke `--help-*` dark palette → standard `.ab-section`/`.ab-card`/`.ab-btn` + `--ab-*` tokens (fixes the light-theme dark-blue bleed). Per Bojan: NO website doc deep-links yet (kept only external validators + "Contact support" → /support). Verified both themes (`artifacts/ui-audit/{light,dark}/11-help.png`). **Both deferred deep tasks now done — only the final page-by-page review remains.** Prior today: **Schema Types fix (v0.87.43, committed `2c1ea07` + pushed; CI parity-guard fixed `092164b` → green).** Bojan Schema #4 deferred task. `TYPE_META` in `vue-admin/src/tabs/SchemaTab.vue` only covered 17 of 36 selectable types → the other 19 (Resort, Bakery, Cafe, Bar, Physician, Pharmacy, Hospital, Vet, B&B, all 5 beauty/fitness salons, Accounting, Childcare, the 3 Finance types) rendered NO descriptive panel — the help copy *vanished* when you picked them. Added per-type copy (title + 1-line description + 3 field hints, family-consistent) for all 19, plus a never-blank safety net: each `SCHEMA_CATEGORIES` entry got an `anchor` and `selectedTypeMeta` falls back to the category anchor → Organization. **UI-copy only — no server/manifest/gating change** (SchemaBuilder + manifest already handled all 36). Verified on staging both themes via new `scripts/verify-schema-type-panel.mjs` (8 types × 2 themes, all show coherent panels → `artifacts/schema-types/{light,dark}/`). Remaining deferred admin-UI task: **Help & Troubleshooting page redesign**, then the final page-by-page review. |
| **Milestone** | Implementing the Instrument mockup (`screens.html`) into the real Vue admin. Phases 1–5 DONE. **Phase 5 correction (v0.87.18–19)** — stripped Joomla outer wrapper padding (`.main-card`, Bootstrap col/row/container) so the component sits flush edge-to-edge; restored 1rem sidebar→content gap; removed sidebar border-radius. JS fallback in AppShell.vue walks the DOM and zeros all Bootstrap wrapper padding (works regardless of Joomla version). Verified both themes on staging v0.87.19. **Header pass (v0.87.20–22)** — imported the authoritative `screens.html` from the claude.ai design project (id `953d98be-…`, "UI mockup collection"); fixed Redirects/Analyzers pill sub-tabs → flat `.ab-tab` strip; moved the "no settings backup" reminder off every page to Dashboard-only (per design); Settings title now shows the active tab name (not generic "Settings"); added a shared `PageHeader` (Instrument `.ab-topbar`) to ALL pages — bold title + right-aligned actions slot + optional subtitle. NB: uppercase section/card/tab labels are INTENTIONAL (Instrument signature in `ab-components.css`), not a bug. Verified both themes on staging v0.87.22. **Design round 2 (v0.87.23–24, plan `dizajner-i-ja-smo-async-muffin.md`)** — applied the Designer's round-2 changes (committed `d5c5d4c` was the header pass; round 2 is NEW, uncommitted): font scale up (2-dec rems), IBM Plex Mono 700, badges (no dot, bold, light-stroke/dark-solid), tags bold, NEW `OnOffSwitch` (ON/OFF labelled), **light sidebar in light theme** + **single-open accordion** groups + wider/bigger items, Dashboard module cards = framed ENABLED/DISABLED + ON/OFF switch (instant toggle, transient flash) in a 3-col grid, Integrations = status label by name + top-right ON/OFF switch + no card icon, removed superfluous Schema sub-tab emoji. Verified both themes on staging v0.87.24. |
| **Next gate** | **Round 3 (v0.87.25, uncommitted)** done + verified: sidebar nav group titles → Title Case (`navigation.js`; Bojan: navbar shouldn't be uppercase — content section/tab labels stay uppercase); sidebar brand shows a **PRO/FREE** edition tag (`boot.isPro`) instead of the version chip (version stays in the Joomla toolbar); **rendered all 22 Designer screens** (light+dark) to `artifacts/designer/` via `scripts/render-designer-screens.mjs` for side-by-side vs `artifacts/ui-audit/`. Designer renders refreshed from the LATEST `screens.html` (`artifacts/designer/{light,dark}/`). **CONFIRMED by rendering: the Designer's sidebar is LIGHT in light theme / DARK in dark, and OURS matches** (my earlier "designer = dark" was a misread of the token file's stale `always-dark` comment — the light sidebar is done inside `screens.html`). Nav group labels kept bigger/bold/Title-Case per Bojan (deliberate deviation from the Designer's small-grey-uppercase). **Tomorrow: per-page polish (Designer vs ours) — still ~60%.** Pending: commit round 3; refresh `handoff/`; styleguide specimens. |

---

## Deployed Versions

Single drift guard — **Free and Pro must match on the test sites**. Refresh with
`python _creds_run.py scripts/install-matrix.py --check` and update this table (and again at the end of any code task).

| Site | Edition | Installed | Verified |
|---|---|---|---|
| staging.offroadserbia.com (live) | Pro | `0.88.0` | 2026-06-29 — T1·S8 (order 0029): per-page noindex mechanism, opt-in default-OFF; Health 94 (unchanged) + new info entry present. Installed via `pkg_aiboost_pro-0.88.0.zip` |
| offroadbalkans.com (live) | Free | `0.88.0` | 2026-06-29 — T1·S8 (order 0029): installed (default-OFF → functionally byte-identical to before). release-only — do not routine-touch |
| joomla6-pro.testmyweb.info | Pro | `0.88.0` ✅ | 2026-06-29 — T1·S8 ON-behaviour golden diff (clean no-4SEO): Markdown alternate noindex when ON, HTML unchanged; restored to OFF default |
| joomla6-free.testmyweb.info | Free | `0.85.4` ✅ | 2026-06-18 — front-end verified (17/17; 4 JSON-LD nodes) |
| joomla5-pro.testmyweb.info | Pro | `0.85.4` ✅ | 2026-06-18 — installed (3/3, 229 fields) |
| joomla5-free.testmyweb.info | Free | `0.85.4` ✅ | 2026-06-18 — installed (3/3, 218 fields) |

> **Whole test matrix CURRENT at `0.85.4` (2026-06-18):** Free (j5free/j6free) and Pro (j5pro/j6pro) all
> refreshed and confirmed. Pro ZIP `pkg_aiboost_pro-0.85.4.zip` (695 KB) built via
> `build-package-zip.py --target all` — STRICT Pro-leakage check passed (no Pro code in the Free packages).
> j6 deep-verified on the front-end (17/17 read-only checks each); the Pro site emits visibly more than Free
> (8 vs 4 JSON-LD nodes, 4067 vs 2844-byte llms.txt). The **live** Free site (offroadbalkans) stays at
> `0.77.2` by design — release only.
>
> **Free test sites confirmed genuinely Free (2026-06-18):** j6free/j5free admin bootstrap reports
> `isPro=false` / `proActivated=false`, and the DB settings carry no activation (`pro_activated` absent,
> all `pro_skus` false) — so the SPA `<ProGate>` locks Pro surfaces exactly as a real Free customer sees.
> The earlier scary "core-Pro active" reading was a **stale check in `verify-frontend-emission.py`**: it
> inferred Pro from the per-field `locked` flag, but core `tier=pro` fields stopped being field-locked in
> the v0.5 one-product transition (`Manifest\Registry::applyLockState`), so that signal reported Pro on
> every site. Fixed to read the authoritative bootstrap `isPro` — it now shows Free=`core Free`,
> Pro=`core-Pro active`. No data change was needed.

---

## What's left to launch (Faza C → 1.0.0)

Reframed 2026-06-18 around the **website + self-hosted licensing backend** project (plan approved;
`…/.claude/plans/ovo-se-malo-zagu-valo-valiant-bachman.md`). Payment is externally blocked/undecided
(LS won't allow test payments, Stripe-from-Serbia uncertain, PayPal/local fallback) → we build a
**payment-agnostic** licensing authority on the Contabo VPS so a **Manual-issued key unblocks full E2E
QA now**, with payment plugged in later.

1. **Track A — website redesign:** migrate aiboostnow.com to Astro (SSG, en + 6 langs), correct
   pricing/content, build `/account` + `/eula`, keep the plugin's URL contract. Visuals via Claude Design.
2. **Track B — backend on VPS:** `api.aiboostnow.com` (license validate/activate/heartbeat/reconcile +
   Manual key issuance) + `updates.aiboostnow.com` (token-gated update/download server). PHP + MariaDB.
3. **Track C — plugin wiring:** point manifests at `updates.`, stop stripping `<updateservers>` from Pro,
   repoint validation authority to `api.`.
4. **E2E QA (manual key):** issue key → activate Pro on staging → save/reload (Pro persists) → System →
   Update shows the Pro feed → one-click update installs.
5. Bump to **`1.0.0`**, lockstep Free+Pro build, full Definition of Done QA, release.

**Corrected:** `EXPECTED_STORE_ID` is **not** null — it is `367944` with all 5 product IDs filled
(`LicenseValidator.php`, configured 2026-06-16). Remaining LS items: re-confirm IDs vs the live dashboard,
and a real-key **payment** test once a processor is unblocked. **Open blockers:** VPS access for the
backend; payment-processor decision (does not block A/B/C or manual-key QA).

---

## Next step

1. ✅ **Instrument CSS applied to staging** (2026-06-20, v0.87.4) — petrol-teal palette, IBM Plex fonts,
   very dark sidebar + dark theme. Visual review ongoing with designer.
2. **Next (tomorrow):** designer sends revised screenshots (icons + other elements) → review → decide
   final direction → CSS cleanup (remove Bootstrap utility classes from Vue templates, tačka 6).
3. **Test matrix** (j5/j6 × free/pro) is behind at `0.85.4` — update after design is settled.
4. **Faza C** (release infrastructure → 1.0.0) deferred — see "What's left to launch".

---

## Start / end-of-chat ritual

- **Start:** read this file first.
- **End of any substantive chat:** update **Now** + **Deployed Versions** + **Next step**; bump
  `component/Version.php` if code changed; commit. Don't report "done" without a test-site verify.
