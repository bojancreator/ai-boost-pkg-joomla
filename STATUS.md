# STATUS — AI Boost for Joomla (single live board)

**This is the one place that tells the truth about *where we are now*.** Read it first at the start of
every chat; update it at the end of every chat. It replaces the old `ROADMAP-v0.5.md` as the command post.

- **What's left to do** → `BACKLOG.md`
- **How we work** (rules + Definition of Done) → `OPERATING.md`
- **Why / history** (decisions, past verification) → `ROADMAP-v0.5.md` (ARCHIVE) and `docs/`

---

## Now

| Field | Value |
|---|---|
| **Code version** | `0.87.55` (2026-06-24) — source of truth `component/Version.php` |
| **Branch** | `design/admin-ui-refresh` (PUSHED to `origin`, upstream set); base `main` |
| **Latest (2026-06-24)** | **#14 fixed — 22 default-ON toggles now read ON instead of lying/turning off (v0.87.55).** The Vue admin's `App.vue` DEFAULTS mirrored only a handful of toggles, so every manifest toggle with `default='1'` that was absent from the saved blob rendered OFF in its checkbox while the backend treated it as ON (`?? 1`) — the UI lied, and a plain Save could persist OFF. Fix (frontend-only): mirrored the **22 divergent default-ON keys** into `App.vue` DEFAULTS as `'1'` (enable_schema/sitemap/robots/opengraph/search_action/twitter_cards, article_/website_schema_enabled, page_type_auto_detect, faq_auto_detect, include_articles/categories/menu_items, ping_google/bing, enable_article_og_type, enable_per_article_fields, llmstxt_enabled, aeo_ai_meta_enabled, auto_domain_detection, enable_canonical, redirect_404_log_enabled). New `ManifestDefaultsParityTest` (PHPUnit) derives the default-ON set from the manifests and asserts each is in DEFAULTS — **red-green proven** (remove one → fails naming it; restore → green). **Documented exception:** the 2 integration master toggles (`integration_falang_enabled`/`integration_yootheme_enabled`) are intentionally NOT mirrored — they wait for the BACKLOG "integration master toggles shown locked (upsell)" item — and the test excludes them by name (+ a guard test that the exception can't rot). Verified: PHPUnit **422 green** + standalone 3/3; deterministic merge proof (all 22 ON on load AND after save+reload; old build read all 22 OFF); **staging v0.87.55 subagent eyes-on PASS** — enable_schema/sitemap/robots/opengraph read ON on fresh load and stay ON after a partial save + reload. — Earlier (v0.87.54): **Integration card options now expand INLINE — the modal is gone.** Owner ask: the "Options" button on the Multilingual + YOOtheme Pro cards opened a pop-up modal; replace it with a non-overlay way of opening options, then point the Dashboard "Multilingual site detected" notification at it. Done: `IntegrationsPage.vue` now renders Options as a second inline accordion (same `ab-int-acc` style as "What this does"), reusing the previously-dead `openOpts`/`toggleOpts` scaffolding; the fields + ProGate(pro) + status + "Save options" moved inline; the whole `.ab-int-modal` block + `optionsModalItem`/`openOptions`/`closeOptions` + modal CSS removed. Added `data-int-key` on each card + a deep-link handler in `mounted()`: `#/integrations?open=<key>` auto-expands that card's options and scrolls to it. `NotificationService.php` multilingual notice re-pointed `#/settings?tab=sitemap&field=enable_hreflang` → **`#/integrations?open=falang`**, label "Configure hreflang" → "Open Multilingual options". Cleanup: removed the dead `multilingualBannerHref`/`multilingualBannerTarget` computeds in `DashboardApp.vue` (still pointed at the old sitemap field). Verified on staging both themes via `scratchpad/verify-integrations-inline.mjs` (subagent eyes-on PASS): no modal, Multilingual card auto-expands inline from the deep-link in light+dark, YOOtheme expands inline, Save present, notif href correct. `composer test` 3/3 + Pro-leakage STRICT green. **Open (flagged to owner, NOT done here):** Sitemap tab still has legacy `enable_hreflang`/`hreflang_sitemap` that overlap the Multilingual card's `falang_hreflang_sitemap` — possible later consolidation. — Earlier (v0.87.53): **All plugin/integration toggles now apply without a manual reload.** Owner: "this should hold for ALL plugins." Root cause: the SPA caches each screen's data per-route (`useLegacyGlobals` — `isLegacyGlobalsReady`) and never re-derives on revisit, so a toggle stayed stale elsewhere until a full reload. Fix: added `invalidateLegacyGlobals(url)`; after a successful toggle, both `IntegrationsPage.toggleIntegration` and `DashboardApp.doToggle` invalidate the **Dashboard + Integrations** cached views (NOT Settings — remounting it would discard unsaved edits) so they re-fetch + remount with fresh data on next visit. The per-field Translation UI keeps its instant `multilangActive` optimistic flip (v0.87.52). Verified both themes via `verify-multilang-reactive.mjs` (ON→OFF flips 12 dropdowns→12 hints, no reload; the dark pass also confirmed the cache re-fetched fresh server state on revisit). 420 tests green. — Earlier (v0.87.52): Multilingual toggle reactive. (v0.87.51): Multilingual integration pass — relabel + true enabled-state + Translation-UI gate. Owner follow-up: flipping the Multilingual switch on the Integrations card didn't apply elsewhere until a page reload (the per-field Translation UI stayed stale, because `multilangActive` was loaded once per page). Fix: `IntegrationsPage.vue applyToggle()` now also sets the shared `multilangActive` ref (from `useTranslations`) when the `falang` integration is toggled — so the per-field dropdowns hide/show instantly across the SPA, and it tracks rollback too. Verified both themes via `scripts/verify-multilang-reactive.mjs` (saveToggle intercepted/faked so the live integration is never touched): ON→OFF flips 12 dropdowns → 12 hints with NO reload, and OFF→ON back. — Earlier: **Multilingual integration pass (v0.87.51, verified both themes).** Four owner points on Integrations → Multilang: (a) **renamed** the card "Multilang" → **"Multilingual"** + provider **"Native Joomla & Falang"** (was bare "Falang") — descriptor in `AiBoostIntFalang.php` + static tile in `IntegrationDetectorService.php`; (b) **true enabled-state** — `master_enabled` now requires the bridge plugin actually published in `#__extensions` (was setting-only), so unpublishing `aiboost_int_falang` in Joomla → Plugins correctly flips the card to disabled; (c) **Translation-UI gate** — new server flag `multilang_active` (bridge published + master on + 2+ languages) on `settings.getLanguages`; `TranslationExpander.vue` hides the per-field dropdowns when inactive and shows a compact "Turn on Multilingual to translate →" hint → `#/integrations` (stored translations untouched); (d) verified: ON→12 dropdowns, OFF→12 hints, card reads "Multilingual"/"Native Joomla & Falang". **Deploy note:** the bridge descriptor lives in the Pro plugin `aiboost_int_falang` (NOT in the Free `pkg_aiboost`), so it was deployed via a separate `--integration=falang` build + `install-to-staging --zip` of the `_pro` ZIP. New `scripts/verify-multilang-gate.mjs`; `FalangBridgeParityTest` label assertion updated to "Multilingual". (#1 unpublish→disabled verified by code; live unpublish test skipped to avoid disrupting the live multilingual site.) — Earlier today: **FAQ Manual Items → per-item repeater (v0.87.50).** Schema #4: `SchemaTab.vue` "Manual FAQ Items" was a raw JSON textarea but translations were per-item — inconsistent. Per owner (chose "both per-item"), replaced the JSON textarea with a Question/Answer repeater (Add/remove, per-row Translations expanders) mirroring the existing services repeater (`parseFaqItems`/`faqRows`/deep-watch→`s.faq_items`/`addFaqItem`/`removeFaqItem`). **No storage/consumer change** — `s.faq_items` still serialises to the same `[{question,answer}]` JSON and per-item translation keys (`faq_{idx}_q/_a`) are unchanged, so existing items + translations carry over with zero migration (verified on staging: 2 items + RU translations loaded). New `scripts/verify-faq-repeater.mjs`. — Earlier today: **Notifications panel — Part A done + verified (v0.87.47).** New `component/lib/src/NotificationService.php` assembles a deduped, severity-sorted Dashboard notification list by REUSING cheap existing signals (licence state, ConflictDetector, plugin-enabled state, settings flags, robots.txt read, backup timestamp, setup flags, multilingual) — no new detection engine; deep probes stay on Health. Wired via Dashboard `HtmlView`/`default.php` → `window.aiBoostDashboard.notifications`. `DashboardApp.vue` panel rebuilt data-driven: **critical pinned at top + warnings/info in a collapsible counter panel**, each with Fix deep-links + dismiss (reuses `health.dismiss`). Removed the old hard-coded conflict cards / backup banner / multilingual banner (folded in). **Caught + fixed a false positive:** the sitemap is served dynamically (no static file) so a `file_exists('sitemap.xml')` check would always warn — removed it (sitemap reachability stays a Health-only live probe). 11 `NotificationServiceTest` cases; full suite green (420 tests). Next: **Part B — in-component "What's New" changelog** + post-update highlight; website `/changelog` stays planned. **Part B — in-component "What's New" changelog page + post-update highlight DONE (v0.87.48), verified both themes** (new `changelog.js` single-source + `ChangelogPage.vue` at `/changelog` + nav item; Dashboard "updated to vX" highlight cleared by `dashboard.markVersionSeen` writing `last_seen_version`; website `/changelog` stays planned). (Prior today: Help `c914d3f`; Schema Types `2c1ea07` + CI fix `092164b`.) ⟶ earlier detail: `HelpPage.vue` rebuilt problem-first per Bojan: groups *I have a problem · I want to do something · Is my site ready · Still stuck*; each item = plain "what it means" + one-click buttons into the matching screen (Health/Conflict/Analyzer/Quick Setup/Import/Settings). Dropped the bespoke `--help-*` dark palette → standard `.ab-section`/`.ab-card`/`.ab-btn` + `--ab-*` tokens (fixes the light-theme dark-blue bleed). Per Bojan: NO website doc deep-links yet (kept only external validators + "Contact support" → /support). Verified both themes (`artifacts/ui-audit/{light,dark}/11-help.png`). **Both deferred deep tasks now done — only the final page-by-page review remains.** Prior today: **Schema Types fix (v0.87.43, committed `2c1ea07` + pushed; CI parity-guard fixed `092164b` → green).** Bojan Schema #4 deferred task. `TYPE_META` in `vue-admin/src/tabs/SchemaTab.vue` only covered 17 of 36 selectable types → the other 19 (Resort, Bakery, Cafe, Bar, Physician, Pharmacy, Hospital, Vet, B&B, all 5 beauty/fitness salons, Accounting, Childcare, the 3 Finance types) rendered NO descriptive panel — the help copy *vanished* when you picked them. Added per-type copy (title + 1-line description + 3 field hints, family-consistent) for all 19, plus a never-blank safety net: each `SCHEMA_CATEGORIES` entry got an `anchor` and `selectedTypeMeta` falls back to the category anchor → Organization. **UI-copy only — no server/manifest/gating change** (SchemaBuilder + manifest already handled all 36). Verified on staging both themes via new `scripts/verify-schema-type-panel.mjs` (8 types × 2 themes, all show coherent panels → `artifacts/schema-types/{light,dark}/`). Remaining deferred admin-UI task: **Help & Troubleshooting page redesign**, then the final page-by-page review. |
| **Milestone** | Implementing the Instrument mockup (`screens.html`) into the real Vue admin. Phases 1–5 DONE. **Phase 5 correction (v0.87.18–19)** — stripped Joomla outer wrapper padding (`.main-card`, Bootstrap col/row/container) so the component sits flush edge-to-edge; restored 1rem sidebar→content gap; removed sidebar border-radius. JS fallback in AppShell.vue walks the DOM and zeros all Bootstrap wrapper padding (works regardless of Joomla version). Verified both themes on staging v0.87.19. **Header pass (v0.87.20–22)** — imported the authoritative `screens.html` from the claude.ai design project (id `953d98be-…`, "UI mockup collection"); fixed Redirects/Analyzers pill sub-tabs → flat `.ab-tab` strip; moved the "no settings backup" reminder off every page to Dashboard-only (per design); Settings title now shows the active tab name (not generic "Settings"); added a shared `PageHeader` (Instrument `.ab-topbar`) to ALL pages — bold title + right-aligned actions slot + optional subtitle. NB: uppercase section/card/tab labels are INTENTIONAL (Instrument signature in `ab-components.css`), not a bug. Verified both themes on staging v0.87.22. **Design round 2 (v0.87.23–24, plan `dizajner-i-ja-smo-async-muffin.md`)** — applied the Designer's round-2 changes (committed `d5c5d4c` was the header pass; round 2 is NEW, uncommitted): font scale up (2-dec rems), IBM Plex Mono 700, badges (no dot, bold, light-stroke/dark-solid), tags bold, NEW `OnOffSwitch` (ON/OFF labelled), **light sidebar in light theme** + **single-open accordion** groups + wider/bigger items, Dashboard module cards = framed ENABLED/DISABLED + ON/OFF switch (instant toggle, transient flash) in a 3-col grid, Integrations = status label by name + top-right ON/OFF switch + no card icon, removed superfluous Schema sub-tab emoji. Verified both themes on staging v0.87.24. |
| **Next gate** | **Round 3 (v0.87.25, uncommitted)** done + verified: sidebar nav group titles → Title Case (`navigation.js`; Bojan: navbar shouldn't be uppercase — content section/tab labels stay uppercase); sidebar brand shows a **PRO/FREE** edition tag (`boot.isPro`) instead of the version chip (version stays in the Joomla toolbar); **rendered all 22 Designer screens** (light+dark) to `artifacts/designer/` via `scripts/render-designer-screens.mjs` for side-by-side vs `artifacts/ui-audit/`. Designer renders refreshed from the LATEST `screens.html` (`artifacts/designer/{light,dark}/`). **CONFIRMED by rendering: the Designer's sidebar is LIGHT in light theme / DARK in dark, and OURS matches** (my earlier "designer = dark" was a misread of the token file's stale `always-dark` comment — the light sidebar is done inside `screens.html`). Nav group labels kept bigger/bold/Title-Case per Bojan (deliberate deviation from the Designer's small-grey-uppercase). **Tomorrow: per-page polish (Designer vs ours) — still ~60%.** Pending: commit round 3; refresh `handoff/`; styleguide specimens. |

---

## Deployed Versions

Single drift guard — **Free and Pro must match on the test sites**. Refresh with
`python _creds_run.py scripts/install-matrix.py --check` and update this table (and again at the end of any code task).

| Site | Edition | Installed | Verified |
|---|---|---|---|
| staging.offroadserbia.com (live) | Pro | `0.87.55` | 2026-06-24 — #14: 22 default-ON toggles mirrored into `App.vue` DEFAULTS so the switches read ON and a partial save no longer turns them off (subagent eyes-on PASS). Prior (`0.87.54`): integration card options inline (modal removed); plugin/integration toggles apply without reload. Component via `pkg_aiboost`; bridge descriptor via separately-installed `plg_system_aiboost_int_falang_pro` ZIP |
| offroadbalkans.com (live) | Free | `0.77.2` | release-only — do not routine-touch |
| joomla6-pro.testmyweb.info | Pro | `0.85.4` ✅ | 2026-06-18 — front-end verified (17/17; 8 JSON-LD nodes) |
| joomla6-free.testmyweb.info | Free | `0.85.4` ✅ | 2026-06-18 — front-end verified (17/17; 4 JSON-LD nodes) |
| joomla5-pro.testmyweb.info | Pro | `0.85.4` ✅ | 2026-06-18 — installed (3/3, 229 fields) |
| joomla5-free.testmyweb.info | Free | `0.85.4` ✅ | 2026-06-18 — installed (3/3, 218 fields) |

> **Whole test matrix CURRENT at `0.85.4` (2026-06-18):** Free (j5free/j6free) and Pro (j5pro/j6pro) all
> refreshed and confirmed. Pro ZIP `pkg_aiboost_pro-0.85.4.zip` (695 KB) built via
> `build-package-zip.py --target all` — STRICT Pro-leakage check passed (no Pro code in the Free packages).
> j6 deep-verified on the front-end (17/17 read-only checks each); the Pro site emits visibly more than Free
> (8 vs 4 JSON-LD nodes, 4067 vs 2844-byte llms.txt). The **live** Free site (offroadbalkans) stays at
> `0.77.2` by design — release only.
>
> **Free test sites confirmed genuinely Free (2026-06-18):** j6free/j5free admin bootstrap reports
> `isPro=false` / `proActivated=false`, and the DB settings carry no activation (`pro_activated` absent,
> all `pro_skus` false) — so the SPA `<ProGate>` locks Pro surfaces exactly as a real Free customer sees.
> The earlier scary "core-Pro active" reading was a **stale check in `verify-frontend-emission.py`**: it
> inferred Pro from the per-field `locked` flag, but core `tier=pro` fields stopped being field-locked in
> the v0.5 one-product transition (`Manifest\Registry::applyLockState`), so that signal reported Pro on
> every site. Fixed to read the authoritative bootstrap `isPro` — it now shows Free=`core Free`,
> Pro=`core-Pro active`. No data change was needed.

---

## What's left to launch (Faza C → 1.0.0)

Reframed 2026-06-18 around the **website + self-hosted licensing backend** project (plan approved;
`…/.claude/plans/ovo-se-malo-zagu-valo-valiant-bachman.md`). Payment is externally blocked/undecided
(LS won't allow test payments, Stripe-from-Serbia uncertain, PayPal/local fallback) → we build a
**payment-agnostic** licensing authority on the Contabo VPS so a **Manual-issued key unblocks full E2E
QA now**, with payment plugged in later.

1. **Track A — website redesign:** migrate aiboostnow.com to Astro (SSG, en + 6 langs), correct
   pricing/content, build `/account` + `/eula`, keep the plugin's URL contract. Visuals via Claude Design.
2. **Track B — backend on VPS:** `api.aiboostnow.com` (license validate/activate/heartbeat/reconcile +
   Manual key issuance) + `updates.aiboostnow.com` (token-gated update/download server). PHP + MariaDB.
3. **Track C — plugin wiring:** point manifests at `updates.`, stop stripping `<updateservers>` from Pro,
   repoint validation authority to `api.`.
4. **E2E QA (manual key):** issue key → activate Pro on staging → save/reload (Pro persists) → System →
   Update shows the Pro feed → one-click update installs.
5. Bump to **`1.0.0`**, lockstep Free+Pro build, full Definition of Done QA, release.

**Corrected:** `EXPECTED_STORE_ID` is **not** null — it is `367944` with all 5 product IDs filled
(`LicenseValidator.php`, configured 2026-06-16). Remaining LS items: re-confirm IDs vs the live dashboard,
and a real-key **payment** test once a processor is unblocked. **Open blockers:** VPS access for the
backend; payment-processor decision (does not block A/B/C or manual-key QA).

---

## Next step

1. ✅ **Instrument CSS applied to staging** (2026-06-20, v0.87.4) — petrol-teal palette, IBM Plex fonts,
   very dark sidebar + dark theme. Visual review ongoing with designer.
2. **Next (tomorrow):** designer sends revised screenshots (icons + other elements) → review → decide
   final direction → CSS cleanup (remove Bootstrap utility classes from Vue templates, tačka 6).
3. **Test matrix** (j5/j6 × free/pro) is behind at `0.85.4` — update after design is settled.
4. **Faza C** (release infrastructure → 1.0.0) deferred — see "What's left to launch".

---

## Start / end-of-chat ritual

- **Start:** read this file first.
- **End of any substantive chat:** update **Now** + **Deployed Versions** + **Next step**; bump
  `component/Version.php` if code changed; commit. Don't report "done" without a test-site verify.
